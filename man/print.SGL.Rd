% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.SGL.R
\name{print.SGL}
\alias{print.SGL}
\title{Print a summary of an SGL solution path}
\usage{
\method{print}{SGL}(x, digits = max(3, getOption("digits") - 3), ...)
}
\arguments{
\item{x}{A fitted \code{"SGL"} object.}

\item{digits}{Significant digits used in the printout.}

\item{...}{Additional arguments passed to \code{\link[base]{print}}.}
}
\value{
The fitted object, invisibly.
}
\description{
Prints a short summary of a fitted \code{"SGL"} object: the regression
type followed by a two-column matrix of the \code{lambdas} used in the
regularization path and the number of non-zero coefficients at each
\code{lambda}.
}
\examples{
set.seed(1)
n <- 50; p <- 100; size.groups <- 10
index <- ceiling(1:p / size.groups)
X <- matrix(rnorm(n * p), ncol = p, nrow = n)
y <- X[, 1:5] \%*\% (-2:2) + 0.1 * rnorm(n)
fit <- SGL(list(x = X, y = y), index, type = "linear")
print(fit)

}
\seealso{
\code{\link{SGL}}
}
