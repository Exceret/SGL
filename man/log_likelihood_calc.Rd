% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_likelihood_calc.R
\name{log_likelihood_calc}
\alias{log_likelihood_calc}
\title{Compute the negative Cox partial log-likelihood}
\usage{
log_likelihood_calc(X, beta, death.times, ordered.time)
}
\arguments{
\item{X}{A numeric design matrix with one row per ordered observation.}

\item{beta}{A numeric vector of coefficients of length \code{ncol(X)}.}

\item{death.times}{A numeric vector of the distinct event times.}

\item{ordered.time}{A numeric vector of the observed times sorted in
increasing order, matching the row order of \code{X}.}
}
\value{
The negative partial log-likelihood, a single numeric value.
}
\description{
Computes the negative Breslow partial log-likelihood of a Cox
proportional hazards model for given coefficient values, using the
exponential of the linear predictor as the hazard multiplier.
}
\examples{
set.seed(1)
n <- 30; p <- 3
X <- matrix(rnorm(n * p), ncol = p)
beta <- c(0.5, -0.2, 0.1)
time <- rexp(n)
status <- rbinom(n, 1, 0.6)
ordered.time <- sort(time)
death.times <- unique(time[status == 1])
log_likelihood_calc(X, beta, death.times, ordered.time)

}
